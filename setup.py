from setuptools import setup, find_packages

setup(
    name="gestor_clientes",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Lista tus dependencias aquí si las tienes
        # Por ejemplo: 'tkinter'
    ],
)